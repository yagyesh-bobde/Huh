import React from 'react'

const Button = ( ) => {
    return (
        <button className={`bg-primary px-4 py-2 flex justify-center items-center text-white font-semibold rounded-full`}>
            Sign In
        </button>
    )
}

export default Button
